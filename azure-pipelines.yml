# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    exclude:
      - gh-pages
  paths:
    exclude:
      - docs/
      - tools/

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile CoverageCoveralls Coverage Restore Compile Test --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: TestResults
              pathtoPublish: 'artifacts/TestResults'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: artifacts
              pathtoPublish: 'artifacts'
