# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    exclude:
      - gh-pages
  paths:
    exclude:
      - docs/
      - tools/

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Publish
        displayName: '🚚 Publish'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Pack NuGetPush Restore Compile Test Coverage Restore Compile Pack Publish --skip'
          env:
            COVERALLS_REPO_TOKEN: $(COVERALLS_REPO_TOKEN)
            NUGET_API_KEY: $(NUGET_API_KEY)