# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    exclude:
      - gh-pages
  paths:
    exclude:
      - docs/
      - tools/

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: '⚙️ Compile'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: Test
        displayName: '🚦 Test'
        dependsOn: [ Compile ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Test --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: coverage.xml
              pathtoPublish: 'artifacts/coverage.xml'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: TestResults
              pathtoPublish: 'artifacts/TestResults'
      - job: Coverage
        displayName: '📊 Coverage'
        dependsOn: [ Test ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Coverage --skip'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [ Compile ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: artifacts
              pathtoPublish: 'artifacts'
      - job: Publish
        displayName: '🚚 Publish'
        dependsOn: [ Pack, Coverage ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd NuGetPush Publish --skip'
